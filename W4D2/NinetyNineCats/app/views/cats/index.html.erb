<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>99Cats</title>
  </head>
  <body>
    <h1>Cats List!</h1>
    <ul>
      <% @cats.each do |cat| %>
        <li>
          <%= link_to cat.name, cat_url(cat.id) %>
        </li>
      <% end %>
    </ul>

    <%= link_to "add cat", new_cat_url %>
  </body>
</html>
